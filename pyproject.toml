[project]
name = "code_sage"
version = "0.1.0"
description = "Personalized code review suggestions."
requires-python = ">=3.8"

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-q"
testpaths = ["tests"]
